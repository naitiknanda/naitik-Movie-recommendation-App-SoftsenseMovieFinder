[build]
  # Forces Netlify to jump directly into your project folder first
  base = "cineverse"
  
  # Runs the build script right inside the project folder
  command = "npm run build"
  
  # Points directly to the generated dist folder inside cineverse
  publish = "cineverse/dist"
