[build]
  # Moves into your project folder first
  base = "cineverse"
  
  # Runs the build command from inside cineverse
  command = "npm run build"
  
  # Points directly to the compiled dist folder relative to the base
  publish = "dist"
